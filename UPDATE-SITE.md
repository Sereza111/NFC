# Как обновлять сайт

## 🚀 Быстрое обновление (для ежедневной работы)

### 1. Вносите изменения в код локально

Редактируйте файлы в VS Code / Cursor как обычно.

### 2. Коммит и Push

```bash
git add .
git commit -m "Описание изменений"
git push
```

### 3. Ждите 3-5 минут

GitHub автоматически:
- ✅ Соберет Docker образ
- ✅ Опубликует его в ghcr.io
- ✅ Уведомит о завершении

Следить за процессом: https://github.com/Sereza111/NFC/actions

### 4. Обновите в Portainer

**Вариант A (автоматически):**
- Если включен GitOps в настройках стека - обновится автоматически

**Вариант B (вручную):**
1. Откройте Portainer: http://85.198.84.223:9000
2. **Stacks** → **nfc-site**
3. Нажмите кнопку **Pull and redeploy**

### 5. Готово! ✨

Ваш сайт обновлен на http://85.198.84.223:10002

---

## 📋 Полный workflow

```
┌─────────────┐
│  Локальная  │
│  разработка │
└──────┬──────┘
       │
       │ git push
       ▼
┌─────────────┐
│   GitHub    │ ← Код хранится здесь
└──────┬──────┘
       │
       │ Автоматически запускается
       ▼
┌─────────────┐
│   GitHub    │ ← Сборка Docker образа
│   Actions   │   (3-5 минут)
└──────┬──────┘
       │
       │ Публикация
       ▼
┌─────────────┐
│   ghcr.io   │ ← Хранится готовый образ
└──────┬──────┘
       │
       │ Pull and redeploy
       ▼
┌─────────────┐
│  Portainer  │ ← Управление контейнерами
└──────┬──────┘
       │
       │ Запуск контейнера
       ▼
┌─────────────┐
│  85.198.84  │ ← Сайт работает
│  .223:10002 │
└─────────────┘
```

---

## 🔄 Откат на предыдущую версию

Если что-то пошло не так:

### Способ 1: Через git

```bash
git log --oneline  # Посмотреть историю
git revert HEAD    # Откатить последний коммит
git push
```

### Способ 2: Через Docker образы

В Portainer можно указать конкретную версию образа:

```yaml
image: ghcr.io/sereza111/nfc:main-abc1234  # Конкретный SHA коммита
```

---

## 📊 Мониторинг

### GitHub Actions
https://github.com/Sereza111/NFC/actions
- История всех сборок
- Логи сборки
- Статус текущей сборки

### Portainer
http://85.198.84.223:9000
- Статус контейнеров
- Логи работающего приложения
- Использование ресурсов

### Логи приложения

В Portainer:
1. **Containers** → **nfc-site**
2. **Logs**
3. Выберите количество строк или **Auto-refresh logs**

Или через SSH:
```bash
docker logs -f nfc-site
```

---

## 🛠️ Разработка новых функций

### Создание ветки для фичи

```bash
git checkout -b feature/new-design
# Вносите изменения
git add .
git commit -m "Add new design"
git push -u origin feature/new-design
```

### Тестирование на сервере

GitHub Actions соберет образ для любой ветки:
```yaml
image: ghcr.io/sereza111/nfc:feature-new-design
```

### Слияние в main

```bash
git checkout main
git merge feature/new-design
git push
```

---

## ⚡ Горячие клавиши (в Cursor/VS Code)

- `Ctrl+Shift+G` - Открыть Git панель
- `Ctrl+Enter` - Закоммитить
- `Ctrl+Shift+P` → "Git: Push" - Запушить

---

## 📝 Примеры коммитов

Хорошие коммиты:
```bash
git commit -m "Add delivery cost calculator"
git commit -m "Fix payment validation bug"
git commit -m "Update homepage hero section"
git commit -m "Optimize card rendering performance"
```

Плохие коммиты:
```bash
git commit -m "fixes"
git commit -m "update"
git commit -m "asdf"
```

---

## 🔐 Секреты и переменные окружения

Никогда не коммитьте в Git:
- ❌ Пароли БД
- ❌ API ключи
- ❌ Токены

Все секреты уже настроены в `docker-compose.yml` в Portainer.

При необходимости изменить:
1. **Stacks** → **nfc-site** → **Editor**
2. Измените переменные в разделе `environment:`
3. **Update the stack**

---

**Вопросы?** Проверьте `GITHUB-ACTIONS-SETUP.md` для подробностей.

