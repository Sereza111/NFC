# Настройка автоматической сборки через GitHub Actions

## Как это работает:

1. **При каждом `git push`** автоматически запускается сборка Docker образа
2. Готовый образ публикуется в **GitHub Container Registry** (ghcr.io)
3. **Portainer** использует этот готовый образ (без сборки на сервере)
4. Обновление стека = одна кнопка в Portainer

## Шаг 1: Активация GitHub Packages

Образ будет публиковаться автоматически, но нужно сделать его публичным:

1. После первого push перейдите на страницу вашего репозитория
2. Справа появится раздел **Packages** → нажмите на пакет `nfc`
3. Нажмите **Package settings**
4. В разделе **Danger Zone** → **Change visibility** → выберите **Public**
5. Подтвердите

## Шаг 2: Коммит и Push

Закоммитьте все изменения:

```bash
git add .
git commit -m "Add GitHub Actions for automatic Docker build"
git push origin main
```

## Шаг 3: Проверка сборки

1. Перейдите в ваш репозиторий на GitHub
2. Откройте вкладку **Actions**
3. Вы увидите запущенный workflow "Build and Push Docker Image"
4. Дождитесь завершения сборки (3-5 минут)

## Шаг 4: Настройка Portainer

Теперь в Portainer используйте обновленный `docker-compose.yml`:

### В Portainer:

1. Откройте **Stacks** → **Add stack**
2. Name: `nfc-site`
3. Build method: **Repository**
4. Repository URL: `https://github.com/Sereza111/NFC.git`
5. Repository reference: `refs/heads/main`
6. Compose path: `docker-compose.yml`
7. **GitOps updates**: ✅ Включите (для автообновления)
8. Нажмите **Deploy the stack**

## Обновление сайта

После изменений в коде:

```bash
# Редактируете файлы
git add .
git commit -m "Update something"
git push
```

**Что происходит автоматически:**
1. ✅ GitHub Actions собирает новый образ (3-5 мин)
2. ✅ Образ публикуется в ghcr.io
3. ✅ В Portainer нажимаете **Pull and redeploy** (если GitOps включен, то автоматически)
4. ✅ Сайт обновлен! 🎉

## Ручное обновление в Portainer

Если GitOps не включен:

1. Откройте **Stacks** → **nfc-site**
2. Нажмите **Pull and redeploy**
3. Готово!

## Преимущества:

✅ Не нужно собирать образ на сервере (экономия ресурсов)  
✅ Сборка происходит на GitHub (быстрее и надежнее)  
✅ История всех версий образов  
✅ Откат на предыдущую версию одной кнопкой  
✅ Обновление сайта = `git push`  

## Альтернатива: Docker Hub

Если хотите использовать Docker Hub вместо GitHub Packages:

1. Создайте репозиторий на [hub.docker.com](https://hub.docker.com)
2. Добавьте secrets в GitHub:
   - Settings → Secrets → Actions
   - `DOCKER_USERNAME` = ваш логин на Docker Hub
   - `DOCKER_PASSWORD` = токен доступа
3. Измените `.github/workflows/docker-build.yml`:

```yaml
env:
  REGISTRY: docker.io
  IMAGE_NAME: ваш-username/nfc-site
```

4. В `docker-compose.yml`:

```yaml
image: ваш-username/nfc-site:latest
```

## Troubleshooting

### Ошибка: "pull access denied"

Образ еще приватный. Сделайте его публичным (см. Шаг 1) или настройте авторизацию в Portainer:

1. **Registries** → **Add registry**
2. Type: **GitHub**
3. Username: ваш GitHub username
4. Personal Access Token: создайте на https://github.com/settings/tokens
   - Права: `read:packages`

### GitHub Actions не запускается

Проверьте что Actions включены:
- Settings → Actions → General → **Allow all actions**

### Portainer не может подтянуть образ

1. Проверьте что образ публичный
2. Проверьте имя образа в `docker-compose.yml`
3. В логах Portainer будет точная ошибка

---

**Готово!** Теперь ваш workflow:
```
Код → git push → GitHub Actions → Образ в ghcr.io → Portainer → Сайт онлайн
```

